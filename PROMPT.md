# 요약 규격

`data/<날짜>.json` 을 읽고 `digest/<날짜>.md` 를 쓴다.
JSON의 세 갈래를 각각 별도 섹션으로 낸다.

| 갈래 | JSON 키 | 분량 |
|---|---|---|
| HuggingFace Daily Papers (전날치) | `hf_daily` | **전량** |
| 주요 학회 oral/spotlight | `conference` | 있는 만큼 |
| 내 관심분야 신착 | `interest` | 있는 만큼 |

## 논문 한 편당 형식 — 이 네 항목을 정확히 이 순서로

```markdown
### N. 논문 제목

`arXiv:XXXX.XXXXX` · ▲업보트 · [링크](url) · 관련도 NN

**어떤 문제를 풀려는가** — 한 줄.

**무엇을 처음 한 논문인가** — 한 문장.

**Contribution**
- 기존 방법이 왜 안 되는지를 포함해서 첫 번째
- 두 번째
- 세 번째

**수행한 Task**
- Task 1 — 한 줄
- Task 2 — 한 줄
- (논문이 한 만큼만. 억지로 늘리지 않는다)
```

## 지켜야 할 것

- **한 줄은 한 줄로.** "어떤 문제"와 "무엇을 처음"이 두세 문장으로 불어나면 규격 위반이다.
- **Contribution 불릿 3개는 기존 방법의 한계와 짝지어 쓴다.** "X를 제안했다"만 있으면 안 되고
  "기존 Y는 Z 때문에 안 되므로 X를 제안했다" 꼴이어야 한다. 초록에 한계가 안 적혀 있으면
  그 분야 통념에 비춰 추론하되, 추론한 부분은 "(추정)"으로 표시한다.
- **Task는 논문이 실제로 한 것만.** 초록만으로 알 수 없으면 그렇게 적는다. 지어내지 않는다.
- 초록이 비어 있는 논문은 제목/출처만 적고 `초록 없음 — 원문 확인 필요`라고 표시한다.
- 한국어로 쓴다. 기술 용어(sparse autoencoder, activation patching 등)는 원어 그대로 둔다.
- 학회 논문은 제목 옆에 `ICLR 2025 Oral` 처럼 학회·등급을 붙인다.

## 문서 머리말

```markdown
# YYYY-MM-DD 논문 다이제스트

> 수집 NNNN편 → 고유 NNNN편 · 요약 NN편
> HF Daily(MM-DD) NN편 · 학회 oral NN편 · 관심분야 NN편(풀 NN)

## 오늘 먼저 읽을 것

1. **제목** — 왜 이걸 먼저 읽어야 하는지 한 줄
2. ...
3. ...
```

맨 앞의 "오늘 먼저 읽을 것" 3편은 전체를 요약한 **뒤에** 고른다.
mechanistic interpretability / 모델 내부 관찰 관점에서 실제로 값어치가 큰 순서로.
관련도 점수는 참고만 하고 최종 판단은 내용으로 한다 — 점수는 키워드 매칭일 뿐이다.

마지막에 `also_ran` 목록을 `## 제목만 훑기` 섹션으로 붙인다 (제목 + 링크만).

## 끝나고

`INDEX.md` 맨 위에 한 줄 추가:
`- [YYYY-MM-DD](digest/YYYY-MM-DD.md) — 요약 NN편 · 오늘의 한 편: <제목>`
